services:
  - type: web
    name: ai-lesson-plan
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python api_server.py
    envVars:
      - key: ARK_API_KEY
        sync: false
      - key: ARK_MODEL_ID
        sync: false